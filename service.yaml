apiVersion: v1
kind: Service
metadata:
  name: springbootapp
  labels:
    snapshot: 0.0.3
spec:
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30004
  selector:
    snapshot: 0.0.3
  type: NodePort
