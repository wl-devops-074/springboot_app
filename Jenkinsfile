pipeline{
   agent any
   stages {
  
  stage('build code') {
      steps {
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
        sh 'uname -a'
         
      }
    }
   stage('upload the artfact jar to Nexus') {
      steps {
         sh 'uname -a'
         sh 'uname -a'
         sh 'uname -a'
      }
    }
   stage('build an image Docker') {
      steps {
        
      }
    }
    
   
     
   stage('upload the image to private Registry') {
      steps {
         sh 'uname -a'
         sh 'uname -a'
      }
    }
   stage('Deploy the snapshot of the application in kubernetes cluster') {
      steps {
         sh 'uname -a'
         sh 'uname -a'
         sh 'uname -a'
      }
    }
   } 
    
}
